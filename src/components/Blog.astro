---
const blogPosts = [
  {
    id: 1,
    title: "Rust-da Ownership Konsepti",
    excerpt: "Rust-un ən vacib xüsusiyyətlərindən biri olan ownership sistemini başa düşmək.",
    category: "Rust",
    readTime: "5 dəq",
    date: "Tezliklə",
    tags: ["Rust", "Memory", "Ownership"]
  },
  {
    id: 2,
    title: "Bevy Engine ilə İlk Oyun",
    excerpt: "Rust-un Bevy oyun mühərriki ilə sadə 2D oyun yaratmaq.",
    category: "Game Dev",
    readTime: "8 dəq",
    date: "Tezliklə",
    tags: ["Bevy", "Rust", "2D"]
  },
  {
    id: 3,
    title: "Unreal Engine Blueprint vs C++",
    excerpt: "Blueprint və C++ arasındakı fərqlər və nə vaxt hansını istifadə etmək lazımdır.",
    category: "Unreal",
    readTime: "6 dəq",
    date: "Tezliklə",
    tags: ["Unreal", "Blueprint", "C++"]
  }
];

const categories = ["Hamısı", "Rust", "Game Dev", "Unreal"];
---

<section id="blog" class="section blog">
  <div class="container">
    <div class="section-title">
      <span class="section-number">04</span>
      <h2>Blog</h2>
    </div>
    
    <div class="blog-intro">
      <p>
        Öyrəndiklərimi və təcrübələrimi burada paylaşacam. Hazırda yazılar 
        üzərində işləyirəm - tezliklə burada Rust, oyun inkişafı və 
        proqramlaşdırma haqqında məqalələr tapa biləcəksiniz.
      </p>
    </div>

    <div class="blog-coming-soon">
      <div class="coming-soon-icon">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
          <path d="M12 6v6l4 2"/>
          <circle cx="12" cy="12" r="10"/>
        </svg>
      </div>
      <h3>Blog Tezliklə</h3>
      <p>Yazılar hazırlanır...</p>
    </div>

    <div class="blog-preview">
      <h3 class="preview-title">Planladığım Yazılar</h3>
      <div class="blog-grid">
        {blogPosts.map((post, index) => (
          <article class="blog-card" style={`animation-delay: ${index * 0.1}s`}>
            <div class="blog-card-header">
              <span class="blog-category">{post.category}</span>
              <span class="blog-date">{post.date}</span>
            </div>
            
            <h4 class="blog-title">{post.title}</h4>
            <p class="blog-excerpt">{post.excerpt}</p>
            
            <div class="blog-card-footer">
              <div class="blog-tags">
                {post.tags.slice(0, 2).map((tag) => (
                  <span class="blog-tag">{tag}</span>
                ))}
              </div>
              <span class="blog-read-time">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <circle cx="12" cy="12" r="10"/>
                  <path d="M12 6v6l4 2"/>
                </svg>
                {post.readTime}
              </span>
            </div>
          </article>
        ))}
      </div>
    </div>

    <div class="blog-subscribe">
      <div class="subscribe-content">
        <div class="subscribe-icon">✉️</div>
        <div class="subscribe-text">
          <h4>Yeniliklərdən Xəbərdar Olun</h4>
          <p>Yeni yazılar əlavə edildikdə GitHub-dan izləyə bilərsiniz.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .blog {
    background: var(--color-black);
    position: relative;
  }

  .blog::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--color-gray-800), transparent);
  }

  .blog-intro {
    max-width: 700px;
    margin-bottom: var(--space-3xl);
  }

  .blog-intro p {
    color: var(--color-gray-400);
    font-size: 1.125rem;
    line-height: 1.8;
  }

  .blog-coming-soon {
    text-align: center;
    padding: var(--space-3xl);
    background: var(--color-gray-900);
    border: 1px dashed var(--color-gray-700);
    border-radius: var(--radius-lg);
    margin-bottom: var(--space-3xl);
  }

  .coming-soon-icon {
    width: 64px;
    height: 64px;
    margin: 0 auto var(--space-lg);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-gray-500);
  }

  .coming-soon-icon svg {
    width: 48px;
    height: 48px;
    animation: pulse 3s ease-in-out infinite;
  }

  .blog-coming-soon h3 {
    font-size: 1.5rem;
    color: var(--color-gray-300);
    margin-bottom: var(--space-sm);
  }

  .blog-coming-soon p {
    font-size: 0.875rem;
    color: var(--color-gray-600);
  }

  .preview-title {
    font-size: 1rem;
    color: var(--color-gray-400);
    margin-bottom: var(--space-xl);
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .blog-grid {
    display: grid;
    gap: var(--space-xl);
    margin-bottom: var(--space-3xl);
  }

  @media (min-width: 768px) {
    .blog-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .blog-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .blog-card {
    background: var(--color-gray-900);
    border: 1px solid var(--color-gray-800);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
    transition: all var(--transition-base);
    opacity: 0.7;
  }

  .blog-card:hover {
    border-color: var(--color-gray-700);
    opacity: 1;
    transform: translateY(-4px);
  }

  .blog-card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .blog-category {
    font-family: var(--font-mono);
    font-size: 0.625rem;
    color: var(--color-gray-400);
    padding: var(--space-xs) var(--space-sm);
    background: var(--color-gray-800);
    border-radius: var(--radius-sm);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .blog-date {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--color-gray-600);
  }

  .blog-title {
    font-size: 1.125rem;
    color: var(--color-white);
    line-height: 1.4;
  }

  .blog-excerpt {
    font-size: 0.875rem;
    color: var(--color-gray-500);
    line-height: 1.6;
    flex: 1;
  }

  .blog-card-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: var(--space-md);
    border-top: 1px solid var(--color-gray-800);
  }

  .blog-tags {
    display: flex;
    gap: var(--space-xs);
  }

  .blog-tag {
    font-family: var(--font-mono);
    font-size: 0.625rem;
    color: var(--color-gray-600);
    padding: var(--space-xs) var(--space-sm);
    background: var(--color-black-lighter);
    border-radius: var(--radius-sm);
  }

  .blog-read-time {
    display: flex;
    align-items: center;
    gap: var(--space-xs);
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--color-gray-600);
  }

  .blog-read-time svg {
    width: 14px;
    height: 14px;
  }

  .blog-subscribe {
    background: var(--color-gray-900);
    border: 1px solid var(--color-gray-800);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
  }

  .subscribe-content {
    display: flex;
    align-items: center;
    gap: var(--space-lg);
  }

  .subscribe-icon {
    font-size: 2rem;
  }

  .subscribe-text h4 {
    font-size: 1rem;
    color: var(--color-white);
    margin-bottom: var(--space-xs);
  }

  .subscribe-text p {
    font-size: 0.875rem;
    color: var(--color-gray-500);
  }

  @media (max-width: 640px) {
    .subscribe-content {
      flex-direction: column;
      text-align: center;
    }
  }
</style>
