---
const articles = [
  {
    id: 1,
    title: "Borrow Checker İlə Real Resurs İdarəsi",
    category: "Rust Core",
    timestamp: "Texniki qeydlər",
    slug: "borrow-checker-resource-management",
    abstract: "Ownership modeli sadəcə kompilator xətası deyil. Yazıda resursların (file handle, socket, telemetry span) struktura yerləşdirilməsi, lifetime-ların modul sərhədlərində ötürülməsi və unsafe bloklarının izolyasiyası barədə danışılır.",
    sections: [
      "RAII pattern və Drop impl-ları ilə deterministik sərbəst buraxma",
      "`&mut T` kontraktının concurrency baxımından təhlili",
      "Borrow checker-i aldatmadan interior mutability istifadəsi"
    ]
  },
  {
    id: 2,
    title: "Tokio-da Deterministik Asinxron Axın",
    category: "Async",
    timestamp: "Araşdırma qeydi",
    slug: "tokio-deterministic-async-design",
    abstract: "Tokio runtime-ında structured concurrency qurarkən task-ların nəzarətli şəkildə dayandırılması, geri təzyiq mexanizmi və tracing span-larının idarəsi haqqında praktiki nəticələr.",
    sections: [
      "Cancellation token-ləri və cooperative stop",
      "`select!` makrosu ilə back-pressure",
      "Telemetry: `tracing` + `OpenTelemetry` inteqrasiyası"
    ]
  },
  {
    id: 3,
    title: "Diaqnostika üçün CLI Platforması",
    category: "Tooling",
    timestamp: "Sahə qeydi",
    slug: "diagnostic-cli-platform",
    abstract: "Sistem mühəndisləri üçün hazırlanan CLI-lər sadə `println!`-dən ibarət olmamalıdır. Yazıda `clap`, `miette`, `indicatif` və `tracing` kombinasiyası ilə istifadəçi və operator təcrübəsi eyni vaxtda necə qorunur.",
    sections: [
      "Strukturlaşdırılmış error report formatı",
      "Progress göstəriciləri və uzun müddətli əməliyyatlar",
      "Log səviyyələrinin konsola və fayla paralel ötürülməsi"
    ]
  }
];
---

<section id="blog" class="section blog">
  <div class="container">
    <div class="section-title">
      <span class="section-number">04</span>
      <h2>Texniki Qeydlər</h2>
    </div>
    
    <div class="blog-intro">
      <p>
        Aşağıdakı məqalələr real layihə olmadan aparılan tədqiqatların nəticələridir. 
        Yazılar hazır kod bazasını deyil, tətbiq etmək istədiyim prinsipləri təsvir edir.
      </p>
    </div>

    <div class="blog-grid">
      {articles.map((post, index) => (
        <article class="blog-card" style={`animation-delay: ${index * 0.1}s`}>
          <div class="blog-card-header">
            <span class="blog-category">{post.category}</span>
            <span class="blog-date">{post.timestamp}</span>
          </div>
          <h3 class="blog-title">
            <a href={`/blog/${post.slug}`}>{post.title}</a>
          </h3>
          <p class="blog-abstract">{post.abstract}</p>
          <ul class="blog-points">
            {post.sections.map((item) => (
              <li>{item}</li>
            ))}
          </ul>
          <div class="blog-note">
            Tam mətn şəxsi qeydlərdə saxlanılır; hazır olduqda açıq mənbə kimi paylaşmağı planlaşdırıram.
          </div>
          <a class="blog-link" href={`/blog/${post.slug}`}>
            Tam məqalə
          </a>
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .blog {
    background: var(--color-black);
    position: relative;
  }

  .blog::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--color-gray-800), transparent);
  }

  .blog-intro {
    max-width: 720px;
    margin-bottom: var(--space-3xl);
  }

  .blog-intro p {
    color: var(--color-gray-400);
    font-size: 1rem;
    line-height: 1.7;
  }

  .blog-grid {
    display: grid;
    gap: var(--space-2xl);
  }

  @media (min-width: 768px) {
    .blog-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1200px) {
    .blog-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .blog-card {
    background: var(--color-gray-900);
    border: 1px solid var(--color-gray-800);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
    transition: border var(--transition-base);
  }

  .blog-card:hover {
    border-color: var(--color-gray-600);
  }

  .blog-card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .blog-category {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--color-gray-500);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .blog-date {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--color-gray-600);
  }

  .blog-title {
    font-size: 1.15rem;
    color: var(--color-white);
    line-height: 1.4;
  }

  .blog-title a {
    color: inherit;
  }

  .blog-abstract {
    font-size: 0.95rem;
    color: var(--color-gray-400);
    line-height: 1.7;
  }

  .blog-points {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .blog-points li {
    font-size: 0.85rem;
    color: var(--color-gray-500);
    padding-left: var(--space-lg);
    position: relative;
  }

  .blog-points li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.5em;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--color-gray-600);
  }

  .blog-note {
    font-size: 0.75rem;
    color: var(--color-gray-600);
    font-style: italic;
  }

  .blog-link {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    align-self: flex-start;
    color: var(--color-gray-400);
    border-bottom: 1px solid var(--color-gray-700);
    padding-bottom: var(--space-xs);
    transition: color var(--transition-fast), border-color var(--transition-fast);
  }

  .blog-link:hover {
    color: var(--color-white);
    border-color: var(--color-gray-400);
  }
</style>
