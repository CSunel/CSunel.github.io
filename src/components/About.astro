---
const profileMetrics = [
  { label: "Fokus", value: "Rust Systems Programming" },
  { label: "İş Sahəsi", value: "CLI • Backend • Tooling" },
  { label: "Məkan", value: "Bakı, AZ" }
];

const disciplines = [
  {
    title: "Deterministik Davranış",
    detail: "Memory ownership və borrow checker prinsipinə sadiq qalaraq undefined behavior riskini aradan qaldırıram."
  },
  {
    title: "Ölçülə bilən performans",
    detail: "Profiling, flamegraph analizləri və `cargo bench` nəticələri ilə sübut olunan optimizasiya mərhələləri."
  },
  {
    title: "Sənədləşdirilmiş axın",
    detail: "API səviyyəsində ADR-lər, texniki qeydlər və CI parametrləri ilə dəstəklənən şəffaf proses."
  }
];

const currentTopics = [
  {
    topic: "Async Rust",
    focus: "Tokio runtime, structured concurrency və cooperative cancellation modelləri."
  },
  {
    topic: "Command-line Platforması",
    focus: "`clap`, `miette`, `indicatif`, `tracing` ilə diaqnostika yönümlü CLI-lər."
  },
  {
    topic: "Linux Toolchain",
    focus: "Cross-compilation, musl target-ləri və container-siz deploy strategiyaları."
  }
];
---

<section id="haqqimda" class="section about">
  <div class="container">
    <div class="section-title">
      <span class="section-number">01</span>
      <h2>Haqqımda</h2>
    </div>
    
    <div class="about-grid">
      <div class="about-content">
        <div class="about-text">
          <p class="about-lead">
            Mən Rzalı Tərlan, Rust əsaslı sistem proqramlaşdırması və backend alətləri üzrə müstəqil tədqiqatçıyam. 
            Prioritetim təhlükəsiz, izaholunan və uzun müddət dəstəklənə bilən infrastruktur komponentləri hazırlamaqdır.
          </p>
          
          <p>
            İş prosesim oyun mühərriklərindən çox aşağı səviyyəli utilitlərə yönəlib: fayl sistemi ilə işləyən CLI proqramları, 
            telemetriya agentləri, asinxron xidmətlər. Kod yazarkən deterministik davranış, açıq telemetriya, sərt CI qaydaları 
            və sənədləşdirilmiş interfeysləri əsas şərt kimi qəbul edirəm.
          </p>
          
          <p>
            Hazırda kommersiya layihələrim yoxdur. Vaxtımı Rust ekosisteminin nüvəsini öyrənməyə, eksperimental prototiplər 
            qurmağa və gələcək açıq mənbə töhfələrinə hazırlıq görməyə sərf edirəm.
          </p>
        </div>
        
        <div class="metrics">
          {profileMetrics.map((metric) => (
            <div class="metric-item">
              <span class="metric-label">{metric.label}</span>
              <span class="metric-value">{metric.value}</span>
            </div>
          ))}
        </div>
        
        <div class="principles">
          {disciplines.map((discipline) => (
            <div class="principle">
              <h4>{discipline.title}</h4>
              <p>{discipline.detail}</p>
            </div>
          ))}
        </div>
        
        <div class="topics">
          <h4>Aktiv Mövzular</h4>
          <div class="topics-list">
            {currentTopics.map((item) => (
              <div class="topic-item">
                <span class="topic-title">{item.topic}</span>
                <p>{item.focus}</p>
              </div>
            ))}
          </div>
        </div>
      </div>
      
      <div class="about-visual">
        <div class="visual-frame">
          <div class="frame-corner frame-corner-tl"></div>
          <div class="frame-corner frame-corner-tr"></div>
          <div class="frame-corner frame-corner-bl"></div>
          <div class="frame-corner frame-corner-br"></div>
          
          <div class="visual-content">
            <div class="code-block">
              <div class="code-header">
                <span class="code-dot"></span>
                <span class="code-dot"></span>
                <span class="code-dot"></span>
                <span class="code-title">profile.rs</span>
              </div>
              <div class="code-body">
<pre><code><span class="code-keyword">use</span> <span class="code-type">telemetry</span>::<span class="code-type">Trace</span>;<br/><br/><span class="code-keyword">struct</span> <span class="code-type">EngineerProfile</span> {"{"}<br/>    <span class="code-prop">language</span>: <span class="code-string">"Rust"</span>,<br/>    <span class="code-prop">domains</span>: [<span class="code-string">"systems"</span>, <span class="code-string">"cli"</span>, <span class="code-string">"backend"</span>],<br/>    <span class="code-prop">constraints</span>: <span class="code-type">Constraints</span>,<br/>{"}"}<br/><br/><span class="code-keyword">struct</span> <span class="code-type">Constraints</span> {"{"}<br/>    <span class="code-prop">memory</span>: <span class="code-string">"ownership"</span>,<br/>    <span class="code-prop">deploy</span>: <span class="code-string">"containerless"</span>,<br/>    <span class="code-prop">tooling</span>: <span class="code-string">"cargo + make"</span>,<br/>{"}"}<br/><br/><span class="code-keyword">const</span> <span class="code-type">RZALI</span>: <span class="code-type">EngineerProfile</span> = <span class="code-type">EngineerProfile</span> {"{"}<br/>    <span class="code-prop">language</span>: <span class="code-string">"Rust"</span>,<br/>    <span class="code-prop">domains</span>: [<span class="code-string">"systems"</span>, <span class="code-string">"cli"</span>, <span class="code-string">"backend"</span>],<br/>    <span class="code-prop">constraints</span>: <span class="code-type">Constraints</span> {"{"}<br/>        <span class="code-prop">memory</span>: <span class="code-string">"borrow checker"</span>,<br/>        <span class="code-prop">deploy</span>: <span class="code-string">"static binary"</span>,<br/>        <span class="code-prop">tooling</span>: <span class="code-string">"nix optional"</span>,<br/>    {"}"},<br/>{"}"};<br/><br/><span class="code-keyword">impl</span> <span class="code-type">EngineerProfile</span> {"{"}<br/>    <span class="code-keyword">fn</span> <span class="code-func">instrument</span>(&self) -&gt; <span class="code-type">Trace</span> {"{"}<br/>        <span class="code-type">Trace</span>::<span class="code-func">new</span>(<span class="code-string">"rzali"</span>)<br/>    {"}"}<br/>{"}"} </code></pre>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about {
    background: linear-gradient(180deg, var(--color-black) 0%, var(--color-black-light) 50%, var(--color-black) 100%);
  }

  .about-grid {
    display: grid;
    gap: var(--space-4xl);
    align-items: start;
  }

  @media (min-width: 1024px) {
    .about-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .about-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-3xl);
  }

  .about-text {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }

  .about-lead {
    font-size: clamp(1.25rem, 2vw, 1.5rem);
    color: var(--color-white);
    font-weight: 500;
    line-height: 1.5;
  }

  .about-text p {
    color: var(--color-gray-400);
    line-height: 1.8;
  }

  .metrics {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
    gap: var(--space-lg);
    padding-top: var(--space-xl);
    border-top: 1px solid var(--color-gray-800);
  }

  .metric-item {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
  }

  .metric-label {
    font-size: 0.75rem;
    color: var(--color-gray-600);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .metric-value {
    font-family: var(--font-mono);
    font-size: 1rem;
    color: var(--color-white);
  }

  .principles {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: var(--space-lg);
  }

  .principle {
    border: 1px solid var(--color-gray-800);
    border-radius: var(--radius-md);
    padding: var(--space-lg);
    background: var(--color-gray-900);
  }

  .principle h4 {
    font-size: 0.875rem;
    color: var(--color-white);
    margin-bottom: var(--space-sm);
  }

  .principle p {
    font-size: 0.8125rem;
    color: var(--color-gray-500);
    line-height: 1.6;
  }

  .topics h4 {
    font-size: 0.875rem;
    color: var(--color-gray-500);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: var(--space-lg);
  }

  .topics-list {
    display: grid;
    gap: var(--space-md);
  }

  .topic-item {
    border-left: 2px solid var(--color-gray-700);
    padding-left: var(--space-lg);
  }

  .topic-title {
    font-family: var(--font-mono);
    font-size: 0.875rem;
    color: var(--color-white);
  }

  .topic-item p {
    font-size: 0.8125rem;
    color: var(--color-gray-500);
    margin-top: var(--space-xs);
  }

  .about-visual {
    position: relative;
  }

  .visual-frame {
    position: relative;
    padding: var(--space-xs);
  }

  .frame-corner {
    position: absolute;
    width: 20px;
    height: 20px;
    border-color: var(--color-gray-700);
    border-style: solid;
    border-width: 0;
  }

  .frame-corner-tl {
    top: 0;
    left: 0;
    border-top-width: 1px;
    border-left-width: 1px;
  }

  .frame-corner-tr {
    top: 0;
    right: 0;
    border-top-width: 1px;
    border-right-width: 1px;
  }

  .frame-corner-bl {
    bottom: 0;
    left: 0;
    border-bottom-width: 1px;
    border-left-width: 1px;
  }

  .frame-corner-br {
    bottom: 0;
    right: 0;
    border-bottom-width: 1px;
    border-right-width: 1px;
  }

  .visual-content {
    padding: var(--space-xl);
  }

  .code-block {
    background: var(--color-gray-900);
    border: 1px solid var(--color-gray-800);
    border-radius: var(--radius-md);
    overflow: hidden;
  }

  .code-header {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    padding: var(--space-md) var(--space-lg);
    background: var(--color-black-lighter);
    border-bottom: 1px solid var(--color-gray-800);
  }

  .code-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--color-gray-700);
  }

  .code-dot:nth-child(1) { background: var(--color-gray-600); }
  .code-dot:nth-child(2) { background: var(--color-gray-700); }
  .code-dot:nth-child(3) { background: var(--color-gray-800); }

  .code-title {
    margin-left: auto;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--color-gray-600);
  }

  .code-body {
    padding: var(--space-lg);
  }

  .code-body pre {
    font-family: var(--font-mono);
    font-size: 0.875rem;
    line-height: 1.8;
  }

  .code-keyword { color: var(--color-gray-400); }
  .code-type { color: var(--color-white); }
  .code-prop { color: var(--color-gray-300); }
  .code-string { color: var(--color-gray-500); }
  .code-bool { color: var(--color-gray-400); }
  .code-func { color: var(--color-gray-300); }
</style>
