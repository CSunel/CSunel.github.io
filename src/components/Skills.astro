---
const capabilityGroups = [
  {
    title: "Sistem Səviyyəsi",
    icon: "chip",
    skills: [
      {
        name: "Rust",
        level: 82,
        description: "Ownership modelləşməsi, lifetimes, unsafe blokların audit edilməsi."
      },
      {
        name: "Memory Profiling",
        level: 70,
        description: "`perf`, `heaptrack`, flamegraph və cache miss analizi."
      },
      {
        name: "Linux Internals",
        level: 65,
        description: "procfs, cgroups, io_uring və sistem çağırışları ilə işləmə."
      }
    ]
  },
  {
    title: "Asinxron İnfrastruktur",
    icon: "network",
    skills: [
      {
        name: "Tokio Runtime",
        level: 68,
        description: "Structured concurrency, cancellation, back-pressure dizaynı."
      },
      {
        name: "Observability",
        level: 66,
        description: "`tracing`, `opentelemetry`, log sampling və mətni strukturlaşdırma."
      },
      {
        name: "CLI Platforması",
        level: 75,
        description: "`clap`, `miette`, `indicatif` ilə diaqnostika yönümlü alətlər."
      }
    ]
  },
  {
    title: "Etibarlılıq və Tooling",
    icon: "shield",
    skills: [
      {
        name: "Testing",
        level: 72,
        description: "`cargo nextest`, property-based testlər, fuzzing (cargo-fuzz)."
      },
      {
        name: "Build Sistemi",
        level: 65,
        description: "cargo workspaces, feature flag strategiyası, Make/Nix axınları."
      },
      {
        name: "Dokumentasiya",
        level: 70,
        description: "mdBook, rustdoc, ADR-lər və texniki qeydlər."
      }
    ]
  }
];

const methodTags = [
  "Static binaries",
  "Zero-cost abstractions",
  "Deterministic CI",
  "Tracing-first",
  "Feature flags",
  "Beehive benchmarking",
  "ADR workflow",
  "Security review"
];
---

<section id="bacariqlar" class="section skills">
  <div class="container">
    <div class="section-title">
      <span class="section-number">02</span>
      <h2>Bacarıqlar</h2>
    </div>
    
    <div class="skills-intro">
      <p>
        Fokus sahəm Rust ilə sistem proqramlaşdırmasıdır. Kod bazalarının ölçülə bilən performansa,
        deterministik davranışa və audit imkanı olan arxitekturaya sahib olması üçün çalışıram.
      </p>
    </div>
    
    <div class="skills-grid">
      {capabilityGroups.map((group, catIndex) => (
        <div class="skill-category" style={`animation-delay: ${catIndex * 0.2}s`}>
          <div class="category-header">
            <div class="category-icon">
              {group.icon === "chip" && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                  <rect x="3" y="3" width="18" height="18" rx="2"/>
                  <path d="M9 3v18M15 3v18M3 9h18M3 15h18"/>
                </svg>
              )}
              {group.icon === "network" && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                  <circle cx="12" cy="12" r="3"/>
                  <path d="M12 5V3m0 20v-2m7-7h2M3 12h2m12.95-4.95l1.414-1.414M6.636 17.364l-1.414 1.414m0-12.728L6.636 7.05m10.728 10.314l1.414 1.414"/>
                </svg>
              )}
              {group.icon === "shield" && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                  <path d="M12 3l8 4v6c0 5-3.5 9-8 9s-8-4-8-9V7z"/>
                  <path d="M12 11v4"/>
                  <path d="M10 13h4"/>
                </svg>
              )}
            </div>
            <h3 class="category-title">{group.title}</h3>
          </div>
          
          <div class="category-skills">
            {group.skills.map((skill, skillIndex) => (
              <div class="skill-item" style={`animation-delay: ${(catIndex * 0.2) + (skillIndex * 0.1)}s`}>
                <div class="skill-header">
                  <span class="skill-name">{skill.name}</span>
                  <span class="skill-percent">{skill.level}%</span>
                </div>
                <div class="skill-bar">
                  <div class="skill-progress" style={`--progress: ${skill.level}%`}></div>
                </div>
                <p class="skill-description">{skill.description}</p>
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>
    
    <div class="skills-extra">
      <h3 class="extra-title">Metodologiya Etiketləri</h3>
      <div class="extra-tags">
        {methodTags.map((tag) => (
          <span class="extra-tag">{tag}</span>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
  .skills {
    background: var(--color-black);
    position: relative;
  }

  .skills::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--color-gray-800), transparent);
  }

  .skills-intro {
    max-width: 720px;
    margin-bottom: var(--space-4xl);
  }

  .skills-intro p {
    color: var(--color-gray-400);
    font-size: 1rem;
    line-height: 1.7;
  }

  .skills-grid {
    display: grid;
    gap: var(--space-3xl);
    margin-bottom: var(--space-4xl);
  }

  @media (min-width: 768px) {
    .skills-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1200px) {
    .skills-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .skill-category {
    background: var(--color-gray-900);
    border: 1px solid var(--color-gray-800);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
    transition: all var(--transition-base);
  }

  .skill-category:hover {
    border-color: var(--color-gray-700);
    transform: translateY(-4px);
  }

  .category-header {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    margin-bottom: var(--space-xl);
    padding-bottom: var(--space-lg);
    border-bottom: 1px solid var(--color-gray-800);
  }

  .category-icon {
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-gray-800);
    border-radius: var(--radius-md);
    color: var(--color-gray-400);
  }

  .category-icon svg {
    width: 22px;
    height: 22px;
  }

  .category-title {
    font-size: 1rem;
    font-weight: 600;
    color: var(--color-white);
  }

  .category-skills {
    display: flex;
    flex-direction: column;
    gap: var(--space-xl);
  }

  .skill-item {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .skill-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .skill-name {
    font-family: var(--font-mono);
    font-size: 0.875rem;
    color: var(--color-white);
  }

  .skill-percent {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--color-gray-500);
  }

  .skill-bar {
    height: 4px;
    background: var(--color-gray-800);
    border-radius: 2px;
    overflow: hidden;
  }

  .skill-progress {
    height: 100%;
    width: var(--progress);
    background: linear-gradient(90deg, var(--color-gray-600), var(--color-gray-400));
    border-radius: 2px;
    transform-origin: left;
    animation: progressFill 1s ease forwards;
  }

  @keyframes progressFill {
    from { transform: scaleX(0); }
    to { transform: scaleX(1); }
  }

  .skill-description {
    font-size: 0.75rem;
    color: var(--color-gray-500);
    line-height: 1.5;
  }

  .skills-extra {
    padding-top: var(--space-3xl);
    border-top: 1px solid var(--color-gray-800);
  }

  .extra-title {
    font-size: 0.95rem;
    color: var(--color-gray-400);
    margin-bottom: var(--space-xl);
    font-weight: 500;
  }

  .extra-tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm);
  }

  .extra-tag {
    padding: var(--space-sm) var(--space-md);
    background: var(--color-gray-900);
    border: 1px solid var(--color-gray-800);
    border-radius: var(--radius-sm);
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--color-gray-500);
    transition: all var(--transition-fast);
  }

  .extra-tag:hover {
    border-color: var(--color-gray-600);
    color: var(--color-white);
    transform: translateY(-2px);
  }
</style>
