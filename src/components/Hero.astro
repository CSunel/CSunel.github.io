---
const techStack = ["Rust", "Systems Programming", "Memory Safety", "Linux"];
---

<section class="hero">
  <div class="hero-bg">
    <div class="grid-overlay"></div>
  </div>
  
  <div class="container hero-content">
    <div class="hero-intro fade-in stagger-1">
      <span class="intro-line"></span>
      <span class="intro-text">Systems Programmer</span>
    </div>
    
    <h1 class="hero-title fade-in stagger-2">
      <span class="title-line">Rzalı</span>
      <span class="title-line title-outline">Tərlan</span>
    </h1>
    
    <div class="hero-role fade-in stagger-3">
      <span class="role-text">Rust Developer & Backend Engineer</span>
    </div>
    
    <p class="hero-description fade-in stagger-4">
      Yüksək performanslı sistemlər və etibarlı proqram təminatı arxitekturası üzrə ixtisaslaşıram. 
      Memory safety, concurrency və low-level sistem proqramlaşdırması əsas fokus sahələrimdir.
    </p>
    
    <div class="hero-tech fade-in stagger-5">
      {techStack.map((tech, index) => (
        <span class="tech-tag" style={`animation-delay: ${0.5 + index * 0.1}s`}>
          {tech}
        </span>
      ))}
    </div>
    
    <div class="hero-cta fade-in stagger-5">
      <a href="#blog" class="btn btn-primary">
        <span>Texniki Məqalələr</span>
      </a>
      <a href="#elaqe" class="btn btn-secondary">
        <span>Əlaqə</span>
      </a>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
    padding-top: 100px;
    background-color: var(--color-black);
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .grid-overlay {
    position: absolute;
    inset: 0;
    background-image: 
      linear-gradient(var(--color-gray-900) 1px, transparent 1px),
      linear-gradient(90deg, var(--color-gray-900) 1px, transparent 1px);
    background-size: 40px 40px;
    opacity: 0.1;
  }

  .hero-content {
    position: relative;
    z-index: 1;
  }

  .hero-intro {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    margin-bottom: var(--space-lg);
  }

  .intro-line {
    width: 40px;
    height: 1px;
    background: var(--color-gray-600);
  }

  .intro-text {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--color-gray-500);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .hero-title {
    margin-bottom: var(--space-lg);
    line-height: 1.1;
  }

  .title-line {
    display: block;
  }

  .title-outline {
    -webkit-text-stroke: 1px var(--color-gray-600);
    -webkit-text-fill-color: transparent;
    color: transparent;
    transition: all var(--transition-slow);
  }

  .hero-role {
    margin-bottom: var(--space-xl);
    font-family: var(--font-mono);
  }

  .role-text {
    font-size: 1rem;
    color: var(--color-gray-300);
  }

  .hero-description {
    max-width: 600px;
    color: var(--color-gray-400);
    margin-bottom: var(--space-2xl);
    font-size: 1rem;
    line-height: 1.6;
  }

  .hero-tech {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm);
    margin-bottom: var(--space-2xl);
  }

  .tech-tag {
    padding: var(--space-sm) var(--space-md);
    border: 1px solid var(--color-gray-800);
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--color-gray-500);
    transition: all var(--transition-fast);
  }

  .hero-cta {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-md);
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-md) var(--space-xl);
    font-family: var(--font-mono);
    font-size: 0.875rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    border: 1px solid transparent;
    transition: all var(--transition-fast);
  }

  .btn-primary {
    background: var(--color-white);
    color: var(--color-black);
  }

  .btn-primary:hover {
    background: var(--color-gray-200);
  }

  .btn-secondary {
    border-color: var(--color-gray-700);
    color: var(--color-gray-300);
  }

  .btn-secondary:hover {
    border-color: var(--color-white);
    color: var(--color-white);
  }

  @media (max-width: 768px) {
    .hero {
      padding-top: 80px;
      padding-bottom: 100px;
    }
  }
</style>
